ArrayList:1，本质是一个可以自动根据储存的数据动态调节长度的数组。
		  2，可以储存不同数据类型的元素。
		  3，插入和删除元素时效率较低，推荐使用LinkedList。
HashTable(常用于背包):1，键值对的形式储存，一个键只能储存一个值，一个值可由多个键储存。
					  2，键和值都是Object类型。
Stack：先进后出，后进先出。
Queue：先进先出，后进后出。
泛型集合：避免装箱拆箱问题。